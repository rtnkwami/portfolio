apiVersion: v1
kind: Service
metadata:
  name: atelier-backend
spec:
  selector:
    app.kubernetes.io/name: atelier
    app.kubernetes.io/component: backend
  ports:
  - port: 3000
    targetPort: backend
